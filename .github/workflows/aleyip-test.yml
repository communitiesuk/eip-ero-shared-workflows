name: AleYip Test

on:
  push:

jobs:
  check-bank-holiday:
    uses: communitiesuk/eip-ero-shared-workflows/.github/workflows/bank-holiday-check.yml@aleyip/test-bank-holiday

  job2:
    runs-on: ubuntu-latest
    needs: check-bank-holiday
    steps:
      - env:
          OUTPUT1: ${{needs.check-bank-holiday.outputs.is_bank_holiday}}
        run: echo "$OUTPUT1"
